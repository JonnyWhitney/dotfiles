[Unit]
Description=Run elephant via uwsm
# Ensures the graphical session is ready before starting
After=graphical-session.target

[Service]
Type=exec
ExecStart=/usr/bin/uwsm app -- elephant 
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
